// Copia y pega este archivo para crear otros tipos de integracion
// Mira 'integracionporrectangulos.js' para ver un ejemplo
// Agregá esta linea dentro del <head> de index.html entre 'dibujo.js' e 'index.js' para cargar tu archivo:
// <script src="nombredearchivo.js"></script>

// Obtiene una func(x)
// a (inicio) y b (fin)
// n (cantidad de subdivisiones)
function calcularPorRectangulos(func, a, b, n) {
  var sumatoria = 0;

  for (var k = 0; k < n; k++) {
    // Calcular
    // Dibujar
  }

  // Retornar resultado final
  return sumatoria;
}
